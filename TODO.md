# Project TODOs

## OPD Display
- [x] **Support Multiple Doctor Sessions:** ~~Fixed by flipping the query approach in `OpdController.ts` — now queries sessions first and maps doctor info onto each one. Doctors with multiple shifts show as separate entries, and doctors with no session today are automatically excluded.~~
